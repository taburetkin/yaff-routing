import { routing } from './index.js';
import { normalizeUrl, replaceUrlParams } from './index.js';

routing.normalizeUrl = normalizeUrl;
routing.replaceUrlParams = replaceUrlParams;

export default routing;
